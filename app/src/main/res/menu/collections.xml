<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="@string/collections_sort_by">

        <menu>

            <item
                android:id="@+id/sort_by_title"
                android:title="@string/collections_sort_by_title"
                app:actionLayout="@layout/widget_radio_button" />

            <item
                android:id="@+id/sort_by_date"
                android:title="@string/collections_sort_by_date"
                app:actionLayout="@layout/widget_radio_button" />

            <item
                android:id="@+id/sort_by_popularity"
                android:title="@string/collections_sort_by_popularity"
                app:actionLayout="@layout/widget_radio_button" />

        </menu>

    </item>

    <item
        android:id="@+id/filter_by_type"
        android:title="@string/collections_filter_by_type">

        <menu>

            <item
                android:id="@+id/bookmarked_only"
                android:title="@string/collections_bookmarked_only"
                app:actionLayout="@layout/widget_switch" />

            <item
                android:id="@+id/show_explicit"
                android:title="@string/collections_show_explicit"
                app:actionLayout="@layout/widget_switch" />

        </menu>

    </item>

    <item
        android:id="@+id/filter_by_language"
        android:title="@string/collections_filter_by_language">

        <menu>

            <group android:id="@+id/language_container" />

        </menu>

    </item>

</menu>